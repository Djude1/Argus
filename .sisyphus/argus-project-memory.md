# Argus Project Memory

## Purpose

本檔記錄 Argus 專案可公開的工作記憶與交接規則。禁止寫入 API Key、Token、密碼、私鑰、完整憑證 JSON、個資或原始敏感錯誤內容。

## Current Project Understanding

- Argus 是 SaaS 級網站健檢平台，MVP 包含同網域爬蟲、SEO/AEO/GEO/被動資安掃描、互動式截圖高光工作區、Word 報告與 AI handoff prompt。
- 技術棧固定：React 18/Vite/Tailwind/Zustand/Axios、Django 5/DRF/SimpleJWT、Celery/Redis、Playwright Python async、SQLite dev/PostgreSQL prod、python-docx、Docker Compose。
- Phase 2 才加入 LLM Agent/tool calling；目前 provider 決策以 GLM/MiniMax/Gemini 實測可用性為準。

## Persistent Rules Added By User

- 所有專案重要內容、決策、地雷、API provider 狀態與參考資料，寫進專案內 skill 或 `.sisyphus` 記憶。
- 每做約 20 分鐘，更新 `.sisyphus/argus-handoff.local.md`。
- 做之前先判斷是否符合使用者真正需求。
- 做完後測試；每找到 `n` 個有效錯誤，新增 `n*2` 個不同類型測試。
- 製作時詳細閱讀 `CLAUDE.md`、`開發計畫.md`、`Project_說明.md`。
- GLM API 出問題時，改用 Gemini 或 MiniMax。
- 使用 API Key 前先測試該 key 有哪些模型可用，並選最適合專題的模型。
- 使用者已提供 MiniMax 設定。

## API Status Snapshot

2026-05-20 安全測試摘要：

- MiniMax：models 授權通過，tool-calling smoke test 通過。
- Google Gemini：models 授權通過。
- GLM：`glm-4.7-flash` 與 `glm-4.5-flash` chat/tool-calling smoke test 通過；多數較高階模型當下回 HTTP 429，舊 flash 版本回 HTTP 400。
- Google service account JSON：欄位形狀正確。

每次實際呼叫 API 前仍需重新測試；不得輸出秘密值。

## Project Skill

專案內 Skill 位置：

- `skills/argus-project/SKILL.md`
- `skills/argus-project/references/project-rules.md`
- `skills/argus-project/references/api-provider-workflow.md`
- `skills/argus-project/references/external-references.md`
- `skills/argus-project/references/technology-adoption.md`

後續 Argus 任務應先讀取上述檔案。

## External Technology Adoption

- 已將使用者提供的外部連結轉成 `skills/argus-project/references/technology-adoption.md`。
- MVP 採納：Scrapling 的 crawler reliability 思路、GEOFlow 的任務/後台/provider fallback 思路、aeo-site/Optimeyes 的 AEO score 與 Top Actions、awesome-geo 的 FAST/GEO technical checklist、RTK 的輸出壓縮。
- Phase 2/3 延後：RAG/知識庫、keyword tracking、competitor analysis、brand mention/citation monitoring、多語言與 API integration。
- 明確不採納：反爬繞過、指紋偽裝、代理池規避、外部替代技術棧、自動產生修復程式碼、批量內容污染。

## Consistency Fixes

- 2026-05-20：統一 AI Provider 描述為 MiniMax-M2.7 優先、GLM `glm-4.7-flash` / `glm-4.5-flash` 第二順位、Gemini 分析備援；Project 說明不再要求 OpenRouter/Hermes 模型 endpoint。
- 2026-05-20：修正 `開發計畫.md` 技術棧表格欄位數，避免 Markdown 表格解析錯誤。
- 2026-05-20：統一資料模型命名為 `Page`，移除採納矩陣中的 `CrawledPage` 命名。
- 2026-05-21：修正 `開發計畫.md` 過時清單——截圖 API、robots.txt、前端授權勾選、自訂 User-Agent、same-origin enforcement、scoring 等已完成項目原誤標為未完成。

## Implementation Snapshot

- 2026-05-20：建立 Django 後端骨架於 `backend/`，包含 `config`、`accounts`、`scans` app。
- 2026-05-20：新增核心模型 `User`、`ScanJob`、`Page`、`Finding`、`AgentSession`、`AgentStep`、`AuthorizationConsent`，並產生初始 migrations。
- 2026-05-20：新增 SimpleJWT token endpoint、ScanJob 建立/list/retrieve/status API、Page/Finding list/retrieve API。
- 2026-05-20：ScanJob 建立 API 已強制授權確認、Active 額外授權、明顯第三方重新確認，並記錄 IP、timestamp、user_id、user-agent。
- 2026-05-20：新增 9 項 Django 測試；發現 1 個 create response serializer 錯誤後，新增 2 個回歸測試並修正。
- 2026-05-20：`uv run python backend/manage.py check`、`makemigrations --check --dry-run`、`test apps.scans`、`ruff check backend` 已通過。
- 2026-05-20：新增 Playwright crawler、static scanners、Celery `run_scan_job`、Word report builder、screenshot/report API。
- 2026-05-20：新增 React/Vite/Tailwind/Zustand 前端 MVP，包含註冊/登入、授權 ScanJob form、掃描列表、互動報告、截圖 blob 下載、Word report blob 下載。
- 2026-05-20：新增 `.env.example` 與 `使用說明.md`。
- 2026-05-20：目前後端 13 項測試通過，前端 `npm.cmd run build` 通過。
- 2026-05-20：修正 Playwright browser path，專案預設使用 `.ms-playwright`，避免安裝到使用者層級快取。
- 2026-05-20：使用者明確要求不得再犯全域污染錯誤；已將「Playwright 必須安裝到專案 `.ms-playwright`」寫入 `AGENTS.md`、專案 Skill 與 project rules。
- 2026-05-21：MVP 收尾。爬蟲新增 per-origin RPS 限速（主動 ≤ 2、被動 5）與 401/403/429 blocked 偵測；新增站台層級 GEO FAST 檢查（llms.txt、AI 爬蟲、JS 渲染依賴、語意化區塊、段落可引用性），以 Finding 落地不需 migration。
- 2026-05-21：Django Admin 客製化（後台總覽儀表、Agent token 監控、`AuthorizationConsent` 唯讀）；前端新增 findings 分類/嚴重度篩選並修正截圖高光座標未依縮放換算的對齊 bug。
- 2026-05-21：後端測試由 13 增至 27 項並全數通過；ruff、check、makemigrations --check、前端 build 皆通過。
- 2026-05-21（第二輪）：續做 MVP 周邊收尾——新增 `UserScanQuota`（每月配額預設 20）、`Page.headers`/`element_boxes` 欄位（migration 0002）、`rerun_scan` management command（cache replay）、react-router-dom v7 三路由（/login、/scans、/scans/:scanId）、完整 Docker Compose 配置（web/worker/redis/db/nginx）。後端測試增至 32 項全綠；React Router 與 Docker 設定需使用者手動驗證。
- 2026-05-21（第二輪）：使用者再次強調「禁止全域安裝」；確認本輪所有依賴皆專案層級（npm install 在 frontend/、uv 在 Docker image 內、Playwright 路徑 `.ms-playwright` 或 image 內 `/ms-playwright`）。
- 2026-05-21（第三輪）：使用者要求建立 superuser `1124`/`1124`、一般使用者改 Google OAuth。`1124` 透過 Django shell 繞過密碼驗證器建立（僅限本機 dev DB）；一般使用者登入改用 `/api/auth/google/`（`google-auth` 驗證 ID Token，首次以 email 為 username 自動建立帳號）。`RegisterView` / `/api/auth/token/` / `/api/auth/token/refresh/` 與 `accounts/serializers.py` 一併移除（孤兒）。`GOOGLE_OAUTH_CLIENT_ID` 由 Vite envDir + envPrefix 設定讓 backend 與 frontend 共用同一個變數。`@react-oauth/google` 安裝於 `frontend/node_modules`；`google-auth`、`requests` 透過 `uv add` 進 `.venv`，皆未污染全域。後端測試由 32 增至 36 項全綠。
- 2026-05-21（第四輪）：使用者明確只會跑 3 命令（`docker compose build`/`up`、`runserver`），其餘自動化。改用 data migration `accounts/0002_bootstrap_superuser` 從 `.env` 讀 username/password 自動建 `1124`/`1124`（idempotent，SQLite 與 Postgres 都生效）。Django 直接服務前端 SPA（`config/urls.py` 加 `/assets/` 與 SPA fallback、`TEMPLATES.DIRS` 加 `frontend/dist`），runserver 一個命令就能用整個 React UI。`frontend/src/api.js` `baseURL` 改相對 `/api`。nginx.conf 加 `/static/` 反代到 web。`.env` 內 `DJANGO_SECRET_KEY` 與 `JWT_SECRET_KEY` 強化為 64-byte random。Google Client ID 已從使用者提供的 `client_secret_*.json` 檔名萃取寫入 `.env`（沒讀 JSON 內容，避免 Secret 進 context）。
- 2026-05-21（第五輪，暫時性）：Google OAuth 撞到 `origin_mismatch`，使用者要求加跳過登入後門。新增 `DevLoginView`（`/api/auth/dev-login/`）以 `settings.DEBUG=True` 為前提；DEBUG=False 自動 404。前端 LoginForm 加「🛠️ 跳過 Google 登入」按鈕。5 處 code 區塊都標 `DEV LOGIN BACKDOOR — REMOVE WHEN GOOGLE OAUTH IS FULLY WORKING` 註解，Google origin 生效後請 grep 此字串刪除整塊。測試由 36 增至 40 項全綠。
- 2026-05-21（第六輪）：F5 / 直接輸入網址（deep link）抗性 + Docker Compose 端到端驗證。修補三處破口：(1) axios 401 response interceptor — 過期 token 自動清除並導回 `/login?next=<原路徑>`；(2) `RequireAuth` / `LoginPage` / `LoginForm` 串接 `?next=` 參數，讓使用者未登入直接打 deep link 或 token 過期後 F5 都能登入後回到原頁；(3) `frontend/Dockerfile` 缺 `GOOGLE_OAUTH_CLIENT_ID`（build context 是 `./frontend`，根目錄 .env 進不來）— 改用 docker-compose build args 注入並寫入 `/.env` 給 Vite envDir 讀。Docker compose 5 個 container 全部 up；HTTP smoke 對 `/`、`/scans`、`/scans/123`、`/login?next=...`、`/assets/*`、`/api/scans/`（401）、帶 token 後 `/api/scans/`（200）、`/admin/`（302→200）、`/api/auth/dev-login/`（200）全部通過。aiglasses.qzz.io（Cloudflare + Vite dev server，另一個專案）被確認為 Argus 的掃描目標而非部署位置；其 robots.txt 未阻擋 Argus UA。
- 2026-05-21（第七輪）：實際對 aiglasses.qzz.io 觸發掃描時失敗，UI 顯示「✗ 掃描失敗：Error」。Root cause：`pyproject.toml` 指定 `playwright>=1.48,<2` 但 uv lock 解析到 1.60.0，`Dockerfile` base image 仍是 `mcr.microsoft.com/playwright/python:v1.48.0-jammy`，預裝的 chromium 版本對不上 1.60.0 需要的 `chromium_headless_shell-1223`。修：base image 升到 `v1.60.0-jammy`。同時改善 `tasks.py` 的 except 處理，`error_message` 從 `exc.__class__.__name__`（只存 "Error"）改為 `f"{class}: {str(exc)[:500]}"`，未來失敗在 UI 可診斷。重 build web/worker image 後重新掃描 aiglasses.qzz.io：completed，pages=11、findings=112、overall_score=20（top actions 全是「頁面未使用 HTTPS」，因該站僅 http）。**地雷**：每次 `docker compose up -d` recreate web 後，frontend nginx 會把 `web:8000` 卡在舊 IP → 502 Bad Gateway，需 `docker compose restart frontend` 強制重新解析（未來考慮 nginx resolver + variable-based proxy_pass 修掉根因）。
- 2026-05-21（第八輪）：UI 兩個明確 bug + 報告 UX 改善。(2) Findings 列表 112 筆扁平展開很雜亂（11 頁 × ~10 種 finding 類型），改成「分類+標題」分組顯示：每組顯示嚴重度色塊、CATEGORY pill、總計頁面數，點開展開該群組底下每一頁與 evidence；同類嚴重度取群內最高，群組排序按 SEVERITY_RANK→category→count；自動展開包含當前 selectedFinding 的群組。新增 `FindingsGroupList` 元件、`buildFindingGroups` 純函式（依 `category::title` 鍵分組）、`SEVERITY_RANK` 常數。styles.css 加 `.finding-group*`、`.finding-item*`、`.category-pill.cat-{security,seo,aeo,geo,ux}`、`.screenshot-caption`。(3) ScreenshotCanvas 原永遠載 `pages[0]` 截圖，改根據 `selectedFinding.page` 找對應 Page；無選 / 站台層級 finding（page=null）fallback `pages[0]`；高光框只繪當前 page 的 findings 避免跨頁畫框；新增頁面標題 caption。
- 2026-05-22（第十二輪）：CSS 全域化。styles.css 加 `:root` 設計 token（深藍 navy 6 階、cyan 強調 4 色、deep 介面文字 2 色、tone good/medium/bad 7 色、圓角 3、陰影 2），把原本散在各 class 寫死的 hex 純色全部換成 `var()`，要換主題色改 `:root` 一處即可（驗證：6 個深藍 hex 在 CSS 內僅出現各 1 次＝只在 `:root` 定義，無殘留無循環）。半透明 glow/border 的 rgba 保留原值（視覺隨主色走）。重複的 utility 組合抽成全域語意 class：`.hint-text`（text-sm text-slate-500，9 處）、`.hint-text-sm`（text-xs text-slate-400，7 處）；只用一次的組合不抽（避免過度抽象）。grep 確認專案執行程式碼本來就零硬編碼絕對路徑（settings.py 用 Path 動態推導、相對路徑、docker 容器內路徑），可攜性無虞；文件/規則檔裡的 `D:\RTK`、`D:\GitHub_Project\Argus` 是環境描述，不影響運作。

- 2026-05-22（第十一輪）：截圖區放大 + 紅框反饋修正。使用者反映詳情頁中央截圖太小（左 sidebar 360 + 右 findings 360 夾擊，截圖實寬僅 ~500px）。`ScanLayout` 改雙模式：list-mode（`/scans`）sidebar inline 360px grid；detail-mode（`/scans/:id`）sidebar 抽離為 fixed drawer overlay（translateX 動畫 + backdrop），主內容拿全寬，截圖可到 ~840px。詳情頁頂部加工具列：`drawer-toggle`（☰ 展開列表/建立掃描）+ `back-to-list-button`。另一回饋：點 finding 沒紅框 — root cause 是該掃描目標多數 finding `bounding_box=null`（站台層級問題如 HTTPS/CSP，加上 aiglasses 是 SPA、scanner 看 server HTML 沒 h1/form/main）。修正：`ScreenshotCanvas` 加 `whole-page-highlight` — selectedFinding 沒 bounding_box（或選到別頁 finding）時，截圖畫整頁紅色 pulse 外框，保證「點了一定有視覺反饋」；元素級 finding 仍畫精確 highlight-box。站台 banner 文字精簡（移除「無對應頁面元素座標」技術說明）。（使用者覺得第九輪後仍單調，要求 dashboard / sidebar / 詳情頁 / 設定頁強化）。新增 4 個純 SVG/CSS 視覺化元件 — `CountUp`（requestAnimationFrame ease-out cubic，整數/小數自適應）、`StackedBar`（水平堆疊比例 + 圖例）、`SeverityBarChart`（5 嚴重度橫條 + 自適應 max scale + glow shadow）、`LineChart`（XY 軸 + 格線 + 點標註 + 數值 + drop-shadow）；以及完整 keyframes（`fade-up`、`bar-grow`、`ring-draw`、`pulse-soft`、`shimmer`、`fade-in`）。Dashboard：StatTile 數字用 CountUp 動畫；加 2 個新 panel（「Findings 嚴重度分佈」用 SeverityBarChart、「各類別 finding 佔比」用 StackedBar，後者需 Promise.all 串接 `/findings-by-category/`）。History：sparkline 升級為 LineChart（含日期 X 軸與分數 Y 軸標註）。掃描列表（sidebar）：scan-card 加左側色帶（依分數 tone good/medium/bad 配漸層 + glow）、加「與該 origin 上一次分數比較」delta 箭頭。掃描詳情：頂部新增 `report-viz` 區（左嚴重度長條、右類別堆疊條）；Top Actions 從純文字段落改為可點按鈕（點了會 selectFinding 自動定位到對應 finding）；改為 gradient 卡片背景。Settings：新 `quota-panel` 含大型配額進度條（漸層 + glow + tone 自適應 good/medium/bad）；新增「累計掃描」「累計 Findings」「技術棧」三張 stat-card；CountUp 在多處。全域動畫：所有 `.stat-tile / .panel / .dashboard-hero` 進場 fade-up 0.4s；`.stat-tile / .scan-card / .history-card` hover translate-y -2px + shadow；bar 動畫 `bar-grow` 0.7s cubic-bezier。前端 bundle 從 262.02 → 270.05 KB（+8 KB），CSS 從 40.51 → 53.15 KB（+13 KB），無額外套件。所有 8 個 SPA 路由仍 200，新 viz class 都正確進 production bundle。**後端新增 4 個 aggregate endpoint**（不需新 model，從 ScanJob/Finding/AuthorizationConsent/UserScanQuota 聚合）：`/api/dashboard/`（總覽 + 各類別平均 + 嚴重度統計 + 最近 5 次 + 本月配額）、`/api/history/`（每 origin 歷次分數 + delta）、`/api/audit/`（scan_created/completed/failed/authorization 事件時間軸）、`/api/findings-by-category/`（跨掃描 finding 按 category::title Counter 聚合）。**前端**：新增 `TopNav`（深色高科技 nav，深藍漸層 + 霓虹 cyan 強調）、6 個 NavLink（Dashboard / 掃描 / 歷史 / 活動 / 分類 / 設定）；新增 5 個分頁 `DashboardPage`（Hero + 4 StatTile + 5 個 category ScoreRing + 最近 5 次列表）、`HistoryPage`（Sparkline SVG + delta tone + 歷次列表）、`AuditPage`（左 timeline marker 風格事件流）、`CategoriesPage`（5 category tab + 重複 finding 列表）、`SettingsPage`（4 settings card + about 區塊）；輔助元件 `ScoreRing`（SVG circle + stroke-dashoffset 動畫 + 三色 tone）、`StatTile`、`Sparkline`。**報告區（FindingsWorkspace）**：頂部加 page tabs（全站/首頁/各頁，顯示該頁 finding 數），URL 用 `?page=<id>` 串接 F5 還原；ScreenshotCanvas 改受控（接收 targetPage prop），不再從 selectedFinding 推算；selectFinding 時自動切到對應 page tab，站台層級 finding 切回「全站」；**(1) 站台級 finding 紅色 banner**：當 selectedFinding 沒 bounding_box 時截圖頂部覆蓋紅色漸層 banner（含 severity / CATEGORY pill / 警示文字 / 備註）。CSS 從 21 → 40 kB（+19 KB，含完整深色 reskin），JS 從 247 → 261 kB（+14 KB）。Routes：`/dashboard /scans /scans/:id /history /audit /categories /settings /login?next=`，預設 `*` redirect 到 `/dashboard`。所有 SPA routes + 4 個新 API endpoint 端到端 smoke 全部 200，後端 40 tests + ruff 通過。**未做（使用者下一輪再評估）**：finding bounding_box 對 H1/img/form/main 以外的元素（CSP、HSTS 等只能站台層級畫 banner）；audit log 改為真正的 model + middleware（目前是從現有資料派生）；同網址歷史的時間軸圖（目前只有 sparkline 與列表）。

- 2026-05-24（第十三/十四/十五輪，Phase 2 全部完成）：T13 Hermes-Agent + T14 主動式資安 + T15 拓撲圖一次推進。**T13**：新增 `apps.agent` 包（providers/tools/loop/findings/runner + management command smoke_providers）；OpenAI-compatible MiniMax/GLM + Gemini 純文字備援 + ProviderChain fallback（RETRYABLE={no_key, network, unsupported, 408, 429, 5xx}）；8 個 tool schema（click/type_text/scroll/get_visible_text/get_dom_summary/take_screenshot/report_ux_issue/finish）；observe-think-act 主迴圈，max_steps=20 + max_tokens=60000 + step_timeout 安全閘；`report_ux_issue` 分離落地（loop 只收集 issues，runner 階段透過 `persist_agent_issues` 寫 Finding，category=ux，含繁中 ai_handoff_prompt 模板要求對方 LLM 不輸出修復程式碼）；同 round 多 tool_calls 只記第一筆 AgentStep token 避免重複；scans/tasks.py 在 SCANNING→AGENT_TESTING→COMPLETED 之間插入 agent 階段，預設 `ARGUS_AGENT_ENABLED=False` 向下相容，agent 失敗只記 warning_summary.agent 不讓掃描失敗；20 項測試（用 TransactionTestCase 避 SQLite async lock）。**T14**：`apps/scans/active_probes.py` 同步 RateLimitedClient（2 RPS throttle、UA SiteSense）+ 三種 probe：admin path enumeration（100 條字典）、open directory listing（GET + Index of 特徵字串）、SQLi boolean-based（每 scan 最多 30 個 query URL、命中 SQL 錯誤特徵 critical，長度差>30%/狀態變動 medium）；tasks.py 在 site_findings 後插入 active 階段，僅 scan_mode=active + active_testing_authorized 時跑；16 項測試。**T15**：ScanJobViewSet 加 @action topology，從 Page.outgoing_links 建 url→id 對照輸出 nodes+edges，node 帶 finding_count/max_severity/tone（critical/high→bad、medium→medium、其餘→good），edges 過濾跨站連結與 self-loop；前端裝 reactflow@11（裝 frontend/node_modules）、新增 TopologyPage 路由 `/scans/:scanId/topology` 包在 ScanLayout 內、節點按 depth 分層手動 layout（x=depth×260）、點節點 navigate 回 `/scans/:id?page=:nodeId`、ScanLayout toolbar 加切換按鈕（detail↔topology）；5 項 API 測試。**Provider 實測**（2026-05-24）：MiniMax-M2.7 tool_calls=yes（209 tokens）、GLM glm-4.5-flash chat 通過但本次 tool_calls=no、GLM 4.6 仍 429、Gemini 44 個 text model 可用。**驗證**：後端測試由 55 → 96 全綠、ruff 通過、frontend build 通過（CSS +28kB / JS +160kB，含 React Flow）。**4 commits 已 commit 未 push**：`8645a8b`（第十一輪 UI）、`01116f0`（T13）、`01bcf6e`（T14）、`1d8d5bd`（T15）。**地雷**：(1) AgentStep.step_number unique per session，一輪多 tool_calls 必須用流水號 counter 不可重用 round number；(2) `asyncio.run + sync_to_async` 寫 SQLite 在測試會 `database table is locked`，loop 測試類別必須用 `TransactionTestCase` 不可用 `TestCase`；(3) Gemini 不接 OpenAI tool calling，schema 不相容，要 fallback 走 chat_text 純文字介面；(4) reactflow 預設右下有 attribution watermark，用 `proOptions={{hideAttribution:true}}` 移除。**未做**：T11 AI 拒絕第三方（要 Agent 上線後加進 system prompt）、T12 Playwright E2E。

- 2026-05-24（拓撲圖重做 + 列表 dedup + 歷史特效）：(1) 使用者反映拓撲圖醜且不像實際結構，**重做**：BFS spanning tree（以 depth=0 為根），每個 subtree 預算 leaf 數，父節點 y = 子節點群中心 → 兄弟對稱不重疊；自訂 TopologyCustomNode 卡片（icon+body+狀態 dot+嚴重度 chip）；首頁節點放大 + cyan 發光 + 🏠；孤島虛線邊 + 📍 塞到樹下方；加 MiniMap（依 tone 染色）；畫布背景改 radial gradient 淡藍紫。(2) **列表 dedup**：`ScanJobViewSet.list` 預設用 subquery `MAX(id) GROUP BY origin` 過濾，每 origin 只回最新一筆；detail/status/topology/report/screenshot 等 detail action 仍能拿任何 id；`?include_history=true` 給歷史頁繞過。(3) **歷史「最新」特效**：HistoryPage 每 origin 第一筆 scan 加 cyan 漸層底 + 邊框 glow 2.8s 呼吸動畫 + 「✨ 最新」chip pulse 1.8s；其他 scan opacity 0.8。(4) ScanList 側欄頂部小字「同網址僅顯示最新一次掃描。查看歷史 →」。**delta 暫不顯示**（list 拿不到舊分數，要 delta 進歷史頁）。**驗證**：後端 96→102 全綠（+6 dedup tests）、ruff 通過、frontend build 通過。**地雷**：(1) `Edit` 工具對 unicode 全形括號（「（）」）與半形匹配嚴格，匹配前要 Read 比對原檔字元；(2) ScanList 的 `previousByOrigin` delta 邏輯依賴「同 origin 多筆在 list」，dedup 後失效，要主動引導使用者去歷史頁看 delta。**2 個 commit**：`35123aa` 拓撲圖重做、`eeee553` 列表 dedup + 歷史特效。

- 2026-05-24（爬蟲進行中動畫 + push）：前面 8 個 commit 已 push 到 origin/main（136661a..eb779d4）。**新增**：CrawlingAnimation 共用元件（蜘蛛 emoji bob + cyan spinner + 漸層 indeterminate 進度條 + 四階段步驟 phase-pulse），套用到 ScanReportPanel 進行中區、ScreenshotCanvas 截圖未就緒 fallback、ScanList scan-card.is-in-progress 加左側流動光帶 + cyan 呼吸陰影。10 個新 keyframes（spider-bob/phase-bob/phase-pulse/crawl-spinner-spin/crawl-indeterminate/crawl-anim-breathe/card-shimmer/card-progress-breathe/...）。CSS 73.83 → 78.08 kB（+4 kB），JS 429.41 → 430.80 kB（+1.4 kB）。**地雷**（複盤）：使用者前一輪反映「拓撲圖沒任何變化」— 根因是 `argus-frontend-1` container 是 nginx 容器，serve image build 時封裝的 dist，本機 `npm run build` 不會反映進容器；改前端後 Docker 模式必須 `docker compose up -d --build frontend` 並 Ctrl+Shift+R 才看得到。本機 runserver 模式（Django 直接服務 frontend/dist）則只需重 build dist + 強制 reload。**1 個 commit 未 push**：`ca8eac7` 爬蟲動畫。

- 2026-05-24（方案 C 真實進度 + ETA + page-tabs 按鈕化）：使用者選方案 C「真實進度百分比」。**後端**：(1) `ScanJob` 加 `progress` JSONField（migration 0003），結構 `{pages_done, pages_total, phase, phase_started_at}`，完成後設 `{}` 避免前端誤判仍進行；(2) `crawler.crawl_site` 加 `progress_callback`（async (done,total)→None），每爬完一頁（含失敗/被擋）呼叫；callback 失敗用 try/except 吞掉，不影響爬蟲；(3) `tasks.py` 新增 `_write_progress` helper（`filter().update()` race-safe 不覆蓋其他欄位）；run_scan_job 三階段（crawling/scanning/agent_testing）各自更新 progress；crawling 階段透過 `sync_to_async(thread_sensitive=True)` 把 async callback 包成可寫 DB；(4) ScanJobSerializer / ScanJobStatusSerializer 暴露 progress 與 started_at；(5) 8 項測試（apps/scans/tests_progress.py）覆蓋 model 預設、兩個 serializer 輸出、helper race-safe、total=0 clamp 為 1、callback signature 與例外吞掉。**前端 CrawlingAnimation**：接收 `progress` + `startedAt` props，每秒 setInterval force 重繪；有 progress 時切換為實質百分比進度條（cyan→indigo gradient + 內側白色 shimmer 從左到右 1.8s）+ 三 chip「已執行 MM:SS · 進度 done/total · X% · 剩餘約 MM:SS」；ETA = phaseElapsed × (total/done − 1)，僅 done>0 且 done<total 顯示；無 progress fallback indeterminate。**page-tabs UI**：從淺灰底切換改為「白底有邊框的可點按鈕」：hover translateY -1px + cyan 邊框 + glow，active cyan 漸層底 + 大 glow + 左側 `▸` 標示，count badge active 變 cyan 實心 + white + glow，cursor-pointer + focus-visible outline。**驗證**：後端 102 → **110/110 全綠**（+8）、ruff 通過、makemigrations 無 drift、frontend build 通過（CSS 79.44→80.49 kB）。**地雷**：(1) `sync_to_async` 從 async callback 寫 DB 必須 `thread_sensitive=True`，否則跨 thread 在 SQLite 會卡 lock；(2) `_write_progress` 必須用 `filter().update()` 而非 `scan_job.save()`，否則會把記憶體中過時的欄位回寫覆蓋掉其他 worker 的修改；(3) 完成階段一定要 `progress = {}` 清空，否則前端會持續顯示「進行中」動畫。**1 個 commit**：`e95ca5c`。

- 2026-05-24（終止掃描 cancel 功能）：使用者主動終止進行中掃描。**設計**：選合作式 cancel 而非 Celery `revoke(terminate=True)`（後者送 SIGTERM 影響同 worker 其他 task；合作式讓 worker 在安全點停下、DB 不留半完成狀態）。**後端**：(1) `ScanJob.Status` 加 `CANCELLED`（migration 0004 alter status choices）；(2) 新 `apps/scans/cancellation.py`：`ScanCancelled` exception + `is_cancelled(scan_job_id)` + `raise_if_cancelled(scan_job_id)` helpers；(3) `crawler.py` 內 `progress_callback` 抓到 `ScanCancelled` 改不吞、往上 propagate；其他 Exception 維持吞掉；(4) `tasks.py` 三階段切換點 `raise_if_cancelled`（crawling callback 每頁、切 scanning 前、scanning 每頁、切 agent 前）；主 try 加 `except ScanCancelled` 分流（不算失敗、補 completed_at、清空 progress、error_message="使用者已終止掃描"、return）；(5) `ScanJobViewSet` 加 `@action POST cancel`：進行中 200 設 CANCELLED、已結束 400、跨使用者 404；(6) 13 項測試（API 8 + helper 5）。**前端**：(1) STATUS_LABELS 加 cancelled（slate ✖）；(2) `CrawlingAnimation` 接 `onCancel` + `cancelBusy` props 顯示「✖ 終止掃描」rose outline 按鈕；(3) `FindingsWorkspace` 加 handleCancel + cancelBusy state，window.confirm 後 POST cancel，不立即 setScan 等 polling；(4) 已終止 banner「已收集到的頁面與 finding 仍保留在下方」。**驗證**：110 → 123 全綠、ruff 通過、frontend build 通過。**地雷**：(1) worker 在 crawling→scanning 切換瞬間若使用者 cancel，沒在切換前 `raise_if_cancelled` 會被 worker 覆蓋回 SCANNING；(2) main except 順序要 ScanCancelled 在 Exception 之前，否則被通用 Exception 攔截變成 failed 而非 cancelled。**1 個 commit**：`e62cb3f`。

- 2026-05-26（**Coin 點數制 + 評論系統 + Jazzmin 後台美化**，全新模組大改造）：使用者要求把月配額換成點數制、做購點頁與評論頁、後台「非常漂亮」。**決策**：付款用「模擬付款」直接加 coin；建立掃描時預扣 max_pages × 10 coin、失敗/取消全額退、完成依實際頁數退差；評論為 Trustpilot 風格（一人一則、admin 可回覆）；退費僅 admin 手動；4 方案 入門 100=100c / 標準 450=500c / 進階 800=1000c / 旗艦 1500=2200c；**移除** UserScanQuota（純 coin 制）、所有人每月自動領 200 coin；後台用 django-jazzmin。**新後端 app `apps.billing`**：`CoinWallet`(balance/total_purchased_ntd/total_scans_used/last_bonus_year+month)、`CoinTransaction`(amount/kind/balance_after/scan_job/plan/admin_actor/note，禁 add/change/delete 維護帳務不可竄改)、`PricingPlan`(4 個 seeded)；`services.py` 是 wallet 唯一寫入入口（grant_monthly_bonus_if_needed/hold_for_scan/refund_full_for_scan/settle_scan_actual/purchase_plan/admin_adjust 全部 `select_for_update` + 冪等）；signal `post_save sender=AUTH_USER_MODEL` 自動建錢包並發首月 200 coin（dispatch_uid 避重註冊）；GoogleLoginView 每次登入更新 last_login + grant_monthly_bonus_if_needed。**API**：GET `/api/billing/wallet/`（餘額+coin_per_page+最近 20 筆 tx）、GET `/api/billing/plans/`、POST `/api/billing/purchase/`、GET `/api/reviews/`（公開）、GET/POST `/api/reviews/mine/`。**ScanJobCreateSerializer**：移除 quota 檢查、改為「coin 不足」400 回 `{coin: "..."}`；validate 通過後 atomic create 內呼叫 hold_for_scan；scan worker 在 COMPLETED 處呼叫 settle_scan_actual（依實際頁數退差 + total_scans_used+1）、`except ScanCancelled` 與 `except Exception` 都呼叫 refund_full_for_scan；cancel API 也呼叫 refund_full_for_scan（refund 函式冪等所以 worker 端再呼叫一次無害）。**新後端 app `apps.reviews`**：`PlatformReview`(user OneToOne, rating 1-5, comment, admin_reply, admin_replied_at/by) + view upsert。**移除**：`UserScanQuota` model/admin/serializers/tests/views.dashboard_summary，migration 0005_delete_userscanquota。**Admin 大改造（jazzmin）**：`uv add django-jazzmin`，INSTALLED_APPS 加 `jazzmin`（在 admin 之前）；JAZZMIN_SETTINGS + JAZZMIN_UI_TWEAKS（indigo navbar/sidebar、flatly theme、`default_theme_mode: auto` 取代被棄用的 `dark_mode_theme`、FontAwesome icons 全模型自訂）；UserAdmin 改寫列表顯示 email/姓名/首登/最後登入/coin 餘額/累積購買/累積掃描；coin_overview readonly field 用 format_html 渲染統計卡片 + 15 筆交易表格（HTML 拆成 summary_html / table_open 兩段常數避免 E501）；4 個 bulk action +100/+500/-100/-500；CoinWalletAdmin 自訂 get_urls 加 `/<int:wallet_id>/adjust/` 自訂 view + template `billing/coinwallet_adjust.html`（漂亮 form 含快捷金額鈕）；CoinTransactionAdmin readonly 帳務；PricingPlanAdmin list_editable is_active；PlatformReviewAdmin 禁 add（只允許使用者建立）、save_model 自動填 admin_replied_at + admin_replied_by。**前端**：store.js 加 `wallet/walletLoading + fetchWallet/setWallet`；api.js 不變；App.jsx 加 `BillingPage`（balance card + 4 plan grid + 模擬付款 + recent transactions table）、`ReviewsPage`（avg/分佈/我的表單/列表 + admin reply 顯示）、`StarRating` 元件；AccountBar 加 `coin-chip`（漸層 indigo→pink 圓角按鈕，顯示餘額，點擊跳 /billing）；ScanJobForm 加 `max_pages` input + `coin-estimate` 區塊（預估 vs 餘額、不足時警告 + 跳購點 CTA）+ submit 後 fetchWallet；DashboardPage 移除 quota 段改 wallet balance + 累積購買；SettingsPage 改成 quota-panel → 點數錢包總覽 + 前往購點按鈕；NAV_ITEMS 加 💎購點 / ⭐評論；Routes 加 `/billing`（需 auth）/ `/reviews`（公開）；styles.css 加大量新樣式（coin-chip、coin-estimate、billing-balance-card、billing-plan-grid、billing-plan-card、reviews-stats、star-rating、review-card、admin-reply 等）。**驗證**：後端 123 → **148/148 全綠**（+25：billing 17 + reviews 8 − quota 3）；ruff All checks passed；frontend build 通過（CSS 80.49→94.55 kB、JS 433→443 kB，未加新套件）。**新模組架構**：`apps/billing/{models,services,signals,serializers,views,urls,admin,tests}.py + migrations/{0001_initial,0002_seed_plans_and_wallets} + templates/admin/billing/coinwallet_adjust.html`；`apps/reviews/{models,serializers,views,urls,admin,tests}.py + migrations/0001_initial`。**地雷**：(1) data migration 內取 settings 要用 `getattr(__import__("django.conf").conf.settings, ...)` 避免 import 時序問題；(2) signal 必須在 `BillingConfig.ready()` 內延後 import 才不會 circular；(3) `select_for_update` 必須在 transaction.atomic 內才生效；(4) signal 對 `settings.AUTH_USER_MODEL` 用字串註冊（不能 import User，會 circular）；(5) jazzmin 3.0.4 的 `dark_mode_theme` 已 deprecated，要改 `default_theme_mode`；(6) `UserScanQuota` migration 仍保留（0002 建表、0005 刪表），測試會自動執行兩者；(7) admin.py format_html 內 inline style 字串超過 100 字會被 ruff 抓 E501，要拆成多段 module-level 常數或 noqa，最乾淨是拆段。**Commits**：尚未 commit（待使用者驗收後一次包）。

- 2026-05-26（**3 步驟結帳 wizard + AI 使用量 dashboard**，第三輪同日）：(1) 使用者要求購買流程拆「選商品 → 填資料 → 確認訂購」3 步驟，且需收姓名/email/發票+條款並存後端。(2) 後續要求把 AI API 使用量呈現到 dashboard。**後端**：新 `apps.billing.PurchaseOrder`（migration 0003）含 user/plan/price+coin 快照/buyer/invoice_type/company_name/tax_id/status pending→paid/transaction FK；`PurchaseRequestSerializer` 加 buyer_name/email/invoice_type/company_name/tax_id/agree_terms，server 驗證公司發票必填 8 碼統編、條款必勾、個人發票自動清空 company 欄位；`PurchaseView` atomic：建 pending order → purchase_plan 入帳 → 連結 tx → 轉 paid → 回 `{wallet, order}`。新 endpoint GET `/api/billing/orders/`、GET `/api/admin/orders/`、GET `/api/admin/dashboard/`（14 天 series + provider_breakdown + top_ai_users）。`/api/admin/overview/` totals 加 ai_tokens_total/ai_sessions_total/ai_tokens_this_month/orders/orders_this_month/orders_paid；`/api/admin/users/{id}/` 多回 `ai_usage` 物件（total_tokens/total_sessions/by_provider，走 AgentSession.scan_job__user 反查避免 user FK 不存在問題）。Jazzmin `PurchaseOrderAdmin` readonly 帳務不可改。**前端**：BillingPage 完全改寫成 3 步驟 wizard（WizardStepper 元件、step 1 plan grid、step 2 form 即時驗證、step 3 review + 應付金額漸層帶、成功 🎉 畫面顯示訂單編號+入帳明細+CTA）；AdminOverviewPage 升級為 dashboard（6 stat card 含 AI tokens、AdminMiniChart 14 天三線時序 SVG、provider 橫條 + tokens/sessions、Top 10 AI 用戶 clickable 表）；AdminUserDetailPage 加「AI 使用量」面板（總 tokens/sessions + by_provider table）。**驗證**：後端 174 → **181 全綠**（+7 PurchaseOrder + 7 AIUsage tests）、ruff 全綠（修 1 個 E501）、frontend build CSS 108→114.61 kB、JS 466→477.64 kB。**地雷**：(a) Edit 工具對中文半/全形標點極敏感（「,」vs「，」），對既存中文檔改寫前先 Read 確認；(b) CSS Edit 跨多輪 conversation 後 harness 認為 stale 要重 Read；(c) Tailwind 3.4+ `:has()` selector 可在 wizard-form 內讓 radio/checkbox 欄位佔整行；(d) AgentSession 無 user FK，AI usage aggregation 必須走 `scan_job__user`；Top users annotate 用 `Sum("scan_jobs__agent_sessions__total_tokens")` 雙跳 related_name。

## Agent Visibility

- 已新增根層 `AGENTS.md`，要求所有進入本專案工作的 agent 先讀 `CLAUDE.md`、`Project_說明.md`、`開發計畫.md`、專案 Skill、references 與本記憶檔。
- 全域入口 `C:\Users\USER\.codex\skills\argus-project\SKILL.md` 只指向專案內正式 Skill，避免專案規則散落到全域。
